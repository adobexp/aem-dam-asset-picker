export type QuickViewMetadataField = {
  /** Normalised property / built-in field id (size, mime, dam:status, adobexp:year, …). */
  id: string;
  /** Display label for the Quick View table row. */
  label: string;
};
